apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: leapfrogai
  namespace: leapfrogai
spec:
  gateways:
  - istio-system/public
  hosts:
  - leapfrogai.leapfrogai.bigbang.dev
  http:
  - route:
    - destination:
        host: api
        port:
          number: 8080
