import type { SvelteComponentTyped } from "svelte";
import type { SvelteHTMLElements } from "svelte/elements";

type RestProps = SvelteHTMLElements["svg"];

export interface GlobalMarketsBarProps extends RestProps {
  /**
   * Specify the pictogram title.
   * @default undefined
   */
  title?: string;

  [key: `data-${string}`]: any;
}

export default class GlobalMarketsBar extends SvelteComponentTyped<
  GlobalMarketsBarProps,
  Record<string, any>,
  {}
> {}
