import { expect, test } from './fixtures';

test("it attaches multiple files", async ({ page }) => {

})

test("it removes the file btn and attached files when switching to an assistant", async ({ page }) => {

})