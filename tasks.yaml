# yaml-language-server: $schema=https://raw.githubusercontent.com/defenseunicorns/uds-cli/v0.14.0/tasks.schema.json

includes:
  - badge: ./tasks/badge.yaml

tasks:
  #############
  # Badge Tasks
  #############
  - name: verify-uds-badge-cpu
    description: "Runs through all CPU UDS bundle packages with the UDS badge verification test"
    actions:
      - task: badge:verify-uds-badge-api
      - task: badge:verify-uds-badge-ui
      - task: badge:verify-uds-badge-llama-cpp-python
      - task: badge:verify-uds-badge-vllm
      - task: badge:verify-uds-badge-text-embeddings
      - task: badge:verify-uds-badge-whisper
      - task: badge:verify-uds-badge-repeater
      - task: badge:verify-uds-badge-supabase

  - name: verify-uds-badge-gpu
    description: "Runs through all GPU UDS bundle packages with the UDS badge verification test"
    actions:
      - task: badge:verify-uds-badge-api
      - task: badge:verify-uds-badge-ui
      - task: badge:verify-uds-badge-vllm
      - task: badge:verify-uds-badge-text-embeddings
      - task: badge:verify-uds-badge-whisper
      - task: badge:verify-uds-badge-supabase
