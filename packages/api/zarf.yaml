# yaml-language-server: $schema=https://raw.githubusercontent.com/zarf-dev/zarf/main/zarf.schema.json

kind: ZarfPackageConfig
metadata:
  description: "LeapfrogAI API"
  name: leapfrogai-api
  version: "###ZARF_PKG_TMPL_IMAGE_VERSION###"

constants:
  - name: IMAGE_VERSION
    value: "###ZARF_PKG_TMPL_IMAGE_VERSION###"

variables:
  - name: EXPOSE_API
    default: "true"
    description: "Flag to expose the OpenAPI schema for debugging."
  - name: DEFAULT_EMBEDDINGS_MODEL
    default: "text-embeddings"

components:
  - name: leapfrogai
    description: "The LeapfrogAI Python API that shadows the OpenAI API specification"
    only:
      flavor: upstream
    required: true
    import:
      path: common
    charts:
      - name: leapfrogai
        valuesFiles:
          - "values/upstream-values.yaml"
    images:
      - "ghcr.io/defenseunicorns/leapfrogai/leapfrogai-api:###ZARF_PKG_TMPL_IMAGE_VERSION###"
      - "ghcr.io/defenseunicorns/leapfrogai/api-migrations:###ZARF_PKG_TMPL_IMAGE_VERSION###"
      - "kiwigrid/k8s-sidecar:1.23.3"

  - name: leapfrogai
    only:
      flavor: registry1
    required: true
    import:
      path: common
    charts:
      - name: leapfrogai
        valuesFiles:
          - "values/registry1-values.yaml"
    images:
      - "registry1.dso.mil/ironbank/opensource/defenseunicorns/leapfrogai/api:v0.10.0"
      # TODO: replace with Ironbank image once hardened: registry1.dso.mil/ironbank/opensource/defenseunicorns/leapfrogai/api/migrations
      - "ghcr.io/defenseunicorns/leapfrogai/api-migrations:###ZARF_PKG_TMPL_IMAGE_VERSION###"
      - "registry1.dso.mil/ironbank/kiwigrid/k8s-sidecar:1.23.3"

  # # TODO: replace with Chainguard image once hardened
  # - name: leapfrogai
  #   only:
  #     flavor: unicorn
  #   required: true
  #   import:
  #     path: common
  #   charts:
  #     - name: leapfrogai
  #       valuesFiles:
  #         - "values/unicorn-values.yaml"
  #   images:
  #     - "ghcr.io/defenseunicorns/leapfrogai/leapfrogai-api:###ZARF_PKG_TMPL_IMAGE_VERSION###"
  #     - "ghcr.io/defenseunicorns/leapfrogai/api-migrations:###ZARF_PKG_TMPL_IMAGE_VERSION###"
  #     - "kiwigrid/k8s-sidecar:1.23.3"
