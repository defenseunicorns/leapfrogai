apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: lfai-vllm-pv-claim
    namespace: leapfrogai
spec:
  storageClassName: "local-path"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 15Gi
